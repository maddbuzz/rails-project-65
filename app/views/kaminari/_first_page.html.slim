li.page-item
  / = link_to_unless current_page.first?, t('views.pagination.first'), url, remote:, class: 'page-link'
  = link_to t('views.pagination.first'), url, remote:, class: "page-link #{'disabled' if current_page.first?}"
